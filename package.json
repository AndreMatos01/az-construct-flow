{
  "name": "az-construct-flow",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "mvnw.cmd quarkus:dev",
    "build": "npm run build --prefix frontend",
    "build:backend": "mvnw.cmd package -DskipTests"
  }
}
